version: '3.8'
services:
  property-management-frontend:
    build:
      context: .  # 指定构建上下文为当前目录（会读取当前目录下的 Dockerfile）
      dockerfile: Dockerfile
    container_name: property-management-frontend
    ports:
      - "8082:8000"  # 将宿主机的 8084 端口映射到容器的 8000 端口
    restart: always  # 容器意外退出时自动重启
